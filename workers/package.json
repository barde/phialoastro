{
  "name": "phialo-design-worker",
  "version": "1.0.0",
  "license": "AGPL-3.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local --live-reload",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production --route 'phialo.de/*' --zone-id \"$CLOUDFLARE_ZONE_ID\"",
    "preview": "wrangler versions upload --experimental-versions",
    "preview:deploy": "wrangler versions deploy --experimental-versions",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "tail": "wrangler tail",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/node": "^24.0.7",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "miniflare": "^4.20250617.4",
    "typescript": "^5.3.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.20.5"
  },
  "dependencies": {
    "itty-router": "^5.0.0"
  }
}
