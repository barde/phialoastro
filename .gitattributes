# Removed LFS tracking - images are small enough for regular Git (21MB total)
# *.jpg filter=lfs diff=lfs merge=lfs -text
# *.png filter=lfs diff=lfs merge=lfs -text
# WebP files are generated at build time, not stored in Git
# *.webp filter=lfs diff=lfs merge=lfs -text
