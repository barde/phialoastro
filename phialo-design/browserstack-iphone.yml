# BrowserStack Configuration for iPhone
projectName: Phialo Design
buildName: Build ${BUILD_ID}
buildIdentifier: ${BUILD_ID}

# Single platform for iPhone tests
platforms:
  - deviceName: iPhone 14 Pro
    browserName: webkit
    osVersion: 16
    deviceOrientation: portrait

# Parallelization settings
parallelsPerPlatform: 5
browserstackLocal: false
localIdentifier: ${BROWSERSTACK_LOCAL_IDENTIFIER}

# Debugging Options
debug: true
networkLogs: true
consoleLogs: errors
testObservability: true

# Framework settings
framework: playwright
playwrightVersion: 1.54.2