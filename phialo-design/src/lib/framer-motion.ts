// Centralized Framer Motion imports for better tree-shaking
// This ensures we only import the specific features we use
export { 
  motion,
  AnimatePresence,
  useInView,
  useAnimation,
  type Variants 
} from 'framer-motion';