---
import { Image } from 'astro:assets';
import Button from '../common/Button.astro';
import { Palette, Eye, Cog } from 'lucide-react';
import { getLocaleFromUrl, createT } from '../../lib/i18n';
import { translations } from '../../lib/translations';

const locale = getLocaleFromUrl(Astro.url);
const t = createT(locale, translations);
---

<section id="services" class="services-section py-24 bg-off-white">
  <div class="container mx-auto px-6">    <!-- Section Header -->
    <div class="text-center mb-20">
      <h2 class="font-display text-4xl md:text-5xl font-medium text-midnight mb-6">
        3D für Sie
      </h2>
      <p class="text-lg text-gray-600 max-w-3xl mx-auto leading-relaxed mb-8">
        <strong>Profitieren Sie von unserer Erfahrung im 3D-Design von Schmuckstücken und Skulpturen.</strong>
      </p>
      <p class="text-lg text-gray-600 max-w-3xl mx-auto leading-relaxed mb-8">
        <strong>Fotorealistische Renderings, die beeindrucken und überzeugen.</strong> Möchten Sie Ihr Schmuckstück besonders gut einschätzen, 
        Ihrem Kunden präsentieren oder für eine Werbeaktion darstellen lassen? 
        Wir erstellen für Sie vor dem Guss auf Wunsch eine hochwertige fotorealistische Vorschau.
      </p>
      
      <!-- YouTube Video Embed -->
      <div class="mb-12 flex justify-center">
        <iframe 
          class="w-full max-w-2xl aspect-video rounded-lg shadow-lg" 
          src="https://www.youtube.com/embed/os0Y88dDGSA?si=gcNPcctpvrc186kW" 
          title="YouTube video player" 
          frameborder="0" 
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
          referrerpolicy="strict-origin-when-cross-origin" 
          allowfullscreen>
        </iframe>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center max-w-6xl mx-auto mb-12">
        <div class="text-left">
          <p class="text-gray-600 mb-4">
            {t('services.intro.germanText')}
          </p>
          <p class="text-gray-600">
            {t('services.intro.englishText')}
          </p>
        </div>
        <div class="flex justify-center">
          <img src="/images/0242_sparklingJewelry_byPhialoDesign_square_569.jpg" alt="Sparkling Jewelry by Phialo Design" class="rounded-lg shadow-lg max-w-sm" />
        </div>
      </div>
    </div>

    <!-- Services Grid -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 lg:gap-12">      <!-- Service 1: 3D Design & CAD -->
      <div class="service-card group">
        <div class="service-icon">
          <Palette className="w-8 h-8 text-gold" />
        </div>
        <h3 class="service-title">
          {t('services.service1.title')}
        </h3>
        <p class="service-description">
          {t('services.service1.description')}
        </p>
        <div class="flex justify-center mb-6">
          <img src="/images/3DfuerSie/3dzuSchmuck.jpg" alt="3D zu Schmuck" class="rounded-lg shadow-md max-w-full" />
        </div>
        <div class="service-cta">
          <Button href="/contact" variant="outline" size="sm">
            {t('services.service1.cta')}
          </Button>
        </div>
      </div>

      <!-- Service 2: Guss & 3D-Druck -->
      <div class="service-card group featured">
        <div class="service-icon">
          <Cog className="w-8 h-8 text-gold" />
        </div>
        <h3 class="service-title">
          {t('services.service2.title')}
        </h3>
        <p class="service-description">
          {t('services.service2.description')}
        </p>
        <p class="text-sm font-medium text-midnight mb-4">
          <strong>{t('services.service2.benefits')}</strong>
        </p>
        <div class="service-cta">
          <Button href="/contact" variant="primary" size="sm">
            {t('services.service2.cta')}
          </Button>
        </div>
      </div>

      <!-- Service 3: Beratung & Support -->
      <div class="service-card group">
        <div class="service-icon">
          <Eye className="w-8 h-8 text-gold" />
        </div>
        <h3 class="service-title">
          {t('services.service3.title')}
        </h3>
        <p class="service-description">
          {t('services.service3.description')}
        </p>
        <div class="flex justify-center mb-6">
          <img src="/images/engRing2_1.jpg" alt="Engagement Ring Design" class="rounded-lg shadow-md max-w-full" />
        </div>
        <div class="service-cta">
          <Button href="/contact" variant="outline" size="sm">
            {t('services.service3.cta')}
          </Button>
        </div>
      </div>
    </div>    <!-- CTA Section -->
    <div class="text-center mt-20">
      <div class="max-w-2xl mx-auto">
        <h3 class="font-display text-2xl font-medium text-midnight mb-4">
          Kontaktieren Sie uns jetzt!
        </h3>
        <p class="text-gray-600 mb-8">
          Benötigen Sie mehr Informationen? Haben Sie eine konkrete Anfrage? Wir beraten Sie gern.
        </p>
        <Button href="#contact" variant="primary" size="lg">
          Jetzt Kontakt aufnehmen
        </Button>
      </div>
    </div>
  </div>
</section>

<style>
  .service-card {
    @apply bg-white rounded-2xl p-8 shadow-sm hover:shadow-lg transition-all duration-300;
    @apply border border-gray-100 hover:border-gray-200;
    @apply transform hover:-translate-y-2;
  }  .service-card.featured {
    @apply ring-2 ring-gold ring-opacity-20 shadow-luxury;
    @apply relative overflow-hidden;
  }
    .service-card.featured::before {
    content: '';
    @apply absolute top-0 left-0 right-0 h-1;
    background: linear-gradient(to right, #D4AF37, rgba(212, 175, 55, 0.6));
  }
  
  .service-icon {
    @apply w-16 h-16 rounded-full flex items-center justify-center mb-6;
    @apply group-hover:bg-gold group-hover:bg-opacity-20 transition-colors duration-300;
    background: rgba(212, 175, 55, 0.1);
  }
  
  .service-title {
    @apply font-display text-xl font-medium text-midnight mb-4;
  }
  
  .service-description {
    @apply text-gray-600 leading-relaxed mb-6;
  }
  
  .service-features {
    @apply space-y-2 mb-8;
  }
  
  .service-features li {
    @apply text-sm text-gray-600 flex items-center;
  }
  
  .service-features li::before {
    content: '✓';
    @apply text-gold mr-3 font-medium;
  }
  
  .service-cta {
    @apply mt-auto;
  }
  
  /* Animation on scroll */
  .service-card {
    opacity: 0;
    transform: translateY(30px);
    animation: fade-in-up 0.6s ease-out forwards;
  }
  
  .service-card:nth-child(1) {
    animation-delay: 0.1s;
  }
  
  .service-card:nth-child(2) {
    animation-delay: 0.2s;
  }
  
  .service-card:nth-child(3) {
    animation-delay: 0.3s;
  }
  
  @keyframes fade-in-up {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>

<script>
  // Intersection Observer for service cards animation
  function initServicesAnimation() {
    const cards = document.querySelectorAll('.service-card');
    
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.style.animationPlayState = 'running';
        }
      });
    }, {
      threshold: 0.2,
      rootMargin: '-50px'
    });
    
    cards.forEach((card) => {
      card.style.animationPlayState = 'paused';
      observer.observe(card);
    });
  }
  
  // Initialize on page load
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initServicesAnimation);
  } else {
    initServicesAnimation();
  }
  
  // Re-initialize on page transitions
  document.addEventListener('astro:page-load', initServicesAnimation);
</script>
